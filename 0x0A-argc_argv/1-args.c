#include <stdio.h>

/* Write a prog that prints number of args passed into it 
 * Followed by a new line
 * 
 * Return: Always 0 Success
 */

	int main(int argc, char *argv[] __attribute__((unused)))

	{
				
		printf("%d\n", argc - 1);

	return(0);
	}
