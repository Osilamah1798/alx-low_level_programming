#include <stdio.h>

/**
 * main - write a prog prints it's name
 * @argc: number of arguments
 * @argv: array of arguments
 * followed by a new line
 *
 * Return: Always 0 (Success)
 */
	int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", *argv);

return (0);
}
